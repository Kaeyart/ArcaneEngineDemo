# Arcane Engine — v1.0.6.1 Compile Fix

- Corrected runtime catalog validation to use LINQ `Any()` for the `IEnumerable` Base Spell, Spell Modifier, and item catalogs.
- Removed the unused `ignored` local from equipment-loadout validation.
- Includes the complete v1.0.6 performance, mouse aim, UI transition, and enemy Health-bar hotfix.
- Uses a unique archive filename to prevent an older browser download from being installed accidentally.
