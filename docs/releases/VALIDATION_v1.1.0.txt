ARCANE ENGINE v1.1.0 DEMO — SOURCE VALIDATION RECORD

Target editor: Unity 6000.5.2f1
Target platform: Linux x86_64
Package: ArcaneEngineDemo_v1.1.0_THE_FORGE_REBORN.zip

Static validation performed before packaging:
- 31 C# source files parsed with the C# tree-sitter grammar.
- 0 syntax-error files.
- 0 deprecated GetInstanceID calls.
- 42 prefix definitions and 42 suffix definitions; all IDs unique.
- 84 standard affix families total.
- 18 Unique item definitions.
- 12 corruption outcomes.
- 69 total item definitions, including more than 48 standard bases.
- New runtime scripts include stable Unity .meta GUIDs.
- Project, build, title, and bundle versions agree on 1.1.0.

Unity full validation available at:
Arcane Engine > Validate 1.1 Demo

The Unity validation gate compiles Base Spells, compiles every Unique against every Base Spell, checks content IDs and hex geometry, generates 2,000 dungeon layouts, generates 10,000 deterministic equipment instances, and verifies rarity caps, legal tiers, legal slots, modifier-group uniqueness, content targets, spell-build validity, and balance caps.

Important: this environment does not contain the Unity Editor, so the final Unity compiler, Play Mode acceptance checklist, and Linux player build must be run locally in Unity 6000.5.2f1.
