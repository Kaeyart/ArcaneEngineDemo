ARCANE ENGINE 1.0 DEMO · SOURCE PACKAGE VALIDATION

Target editor: Unity 6000.5.2f1
Target player: Linux x86_64
Product: Arcane Engine - The Relic Forge
Bundle version: 1.0.6.1
Runtime source: 12,500 lines across 28 runtime C# files
Patch contract: 39/39 numbered implementation sections documented

Completed outside Unity:
- Every runtime and Editor C# file parsed with the C# tree-sitter grammar: 0 syntax errors.
- Duplicate member scan: no invalid duplicate data members detected.
- Package manifest and runtime assembly definition parsed as valid JSON.
- Linux build shell script passed `bash -n`.
- No deprecated `GetInstanceID()` calls remain.
- No deprecated Input Manager or sort-mode object-search calls remain.
- Input System 1.19.0, exclusive input handling, and Dynamic Batching disablement are configured.
- Runtime UI uses dedicated UIDocument hosts and a shared Panel Settings instance with an explicit default-runtime TSS.
- Runtime menu buttons have a direct Input System fallback, keyboard escape routes, and duplicate-click suppression.
- Title input no longer depends on a zero timescale; IMGUI mouse-up and key events independently dispatch visible button actions.
- A bootstrap-level IMGUI event pump backs up held/pressed/released keyboard and mouse gameplay input.
- Live Input System mouse coordinates take priority over intermittent IMGUI events; fallback press/release transitions are consumed exactly once.
- Combat HUD text is rate-limited, enemy view collections are allocation-free in LateUpdate, and health bars execute after camera tracking.
- Active interaction, Breakable Prop, and Persistent Spell Zone registries replace repeated scene-wide searches in gameplay hot paths.
- Normal Play startup runs lightweight catalog/math checks; the 2,000-layout stress test remains part of explicit Editor validation and release builds.
- Generated low-frequency looping music and ambience are disabled; only short event cues remain.
- Existing AudioListeners are normalized before the game camera listener is created.
- ProjectVersion, EditorBuildSettings, product name, and bundle version are present.

Automated inside Unity on import/build:
- Swept combat-plane math.
- Content ID uniqueness.
- Compilation of every Base Spell.
- Hex-board cell and rotation invariants.
- Stable build fingerprints and deterministic seed hashing.
- Armor and resistance caps.
- 2,000 generated room-layout simulations.

Unity Editor is not installed in the packaging environment, so Unity compilation, Play Mode, and the Linux player build must be run with the included Unity 6000.5.2f1 validation/build command. The build pipeline fails before producing a player if the automated Unity validation reports an error.
