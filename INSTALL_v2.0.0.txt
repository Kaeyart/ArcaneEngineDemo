ARCANE ENGINE DEMO 2.0.0 — CUMULATIVE OVERLAY INSTALL

Target Unity version: 6000.5.2f1
Expected archive: /home/kaey/Downloads/ArcaneEngineDemo_v2.0.0_VISUAL_CONTRACT_COMPLETION.zip
Existing project: /home/kaey/Desktop/ArcaneEngineDemo_v1.0.0_DEMO_PATCH/ArcaneEngineDemo

Close Unity and Unity Hub for this project, place the ZIP in Downloads, then paste this entire line into the terminal:

project='/home/kaey/Desktop/ArcaneEngineDemo_v1.0.0_DEMO_PATCH/ArcaneEngineDemo'; archive='/home/kaey/Downloads/ArcaneEngineDemo_v2.0.0_VISUAL_CONTRACT_COMPLETION.zip'; backup="${project}_backup_before_v2.0.0_$(date +%Y%m%d_%H%M%S)"; test -d "$project/Assets/ArcaneEngine" && test -f "$archive" && mkdir -p "$backup/Assets" && cp -a "$project/Packages" "$project/ProjectSettings" "$backup/" && mv "$project/Assets/ArcaneEngine" "$backup/Assets/" && unzip -o "$archive" -d "$project" && printf 'Arcane Engine 2.0.0 installed. Backup: %s\n' "$backup"

This replaces only Assets/ArcaneEngine and overlays Packages plus ProjectSettings. Other user-created Assets remain untouched. The old ArcaneEngine folder and the prior package/project settings are kept in the timestamped backup printed by the command.

After installation:

1. Open the existing project in Unity Hub. Do not create a new project.
2. Open Assets/ArcaneEngine/Scenes/Main.unity.
3. Wait for compilation to finish.
4. Run Arcane Engine > Validate 2.0.0 Demo.
5. Follow PLAY_MODE_ACCEPTANCE_2.0.0.md.

If the install command prints nothing, one of its two safety checks failed: either the existing Assets/ArcaneEngine folder was not found or the ZIP was not present at the exact Downloads path. No project files are moved until both checks pass.
